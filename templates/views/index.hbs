{{!-- HBS is template engine for handlebars, handlebars is not compatible with express but HBS is --}}
{{!-- HBS is just HTML with additional features for rendering content dynamically --}}
{{!-- index.hbs will replace index.html once views has been set via express.set --}}
{{!-- HBS allows us to reuse markup and render content dynamically through partials --}}

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>URL Shortener</title>
  <link rel="stylesheet" type="text/css" href="css/styles.css"> 
</head>
<body>
  <div class="main">
    {{>header}}
    <p>Input a URL and watch it magically become shorter!</p>
    <div class="form-container">
      <form id="form-url">
        <input id="input-url" class="round-border" placeholder="Shorten URL">
        <button class="round-border">Shorten!</button>
      </form>
      <p class="message-url round-border">
        <span class="original-url"></span>
        <span class="short-url-container">
          <span class="short-url"><a></a></span>
          <button id="copy-button" class="round-border">Copy</button>
        </span>
      </p>
    </div>

  </div>
  {{>footer}}
  {{!-- reference files as if index.hbs is inside of public as all static assets are set to serve from that folder --}}
  <script src="js/app.js"></script>
</body>
</html>